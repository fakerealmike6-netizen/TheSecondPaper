# Frontier labels and reference delta

Only the frozen nine actual probe-frontier addresses were targeted, after checking prior successful/empty Meta queries and approved local sources. This R1 adds zero MetaSleuth calls. Dune explicit internally consistent ownership has priority; behavior/protocol labels are not controller identity. Failed lookup is never NO_LABEL, and UNKNOWN does not block an ordinary explainable chain.

Final label status: see manifest. New observation rows: 36; identity changes: 0. Reference relations compared: 10430; changed: 0. Reference B/C and unresolved counts are in the private manifest. No paper denominator is frozen.

The MIN contains row-by-row address/source opportunities, adoption deltas and reference deltas with source hashes. Full third-party labels and reference tables are excluded from this public snapshot.
