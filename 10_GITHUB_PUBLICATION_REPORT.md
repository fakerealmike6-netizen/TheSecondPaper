# Publication protocol and authoritative receipt

Authorized repository: https://github.com/fakerealmike6-netizen/TheSecondPaper . The authenticated normal CLI identified the intended owner and write permission; remote stage1b-r1 still resolved to `9d54ead12c14ac116aae391ab8e5f66e3035d156` before this append. The intended new tag is `stage1b-r2-20260907T105844+0800_stage1b_r2`. No old tag or commit is rewritten.

This frozen payload cannot contain its own eventual commit/ZIP hash. Final local/remote commit identity, tag, Release, public asset download size and SHA-256 are exclusively resolved by the late PUBLICATION_RECEIPT.json and external REVIEW_HANDOFF.md. A receipt is required before calling publication successful; local commit alone is never a remote publication claim.

Only the explicit public tree is staged into the existing public checkout. Private MIN, original evidence archives, third-party raw responses, complete label/reference data, account ledgers and recovery packages are excluded. The Release attaches only the public ZIP and its sidecar. PUBLIC_LOCAL_EQUIVALENCE binds frozen payload paths; generated objects are explicitly excluded from payload equivalence.
