# Open items and stop

Both fixed ETH queries have complete native context within the declared model scope and exact model results. The remaining items are separate: WETH log-to-call-frame evidence is unavailable under the current account tier; final export/inherited uncertain charges are not fully invoiced; Linux was not locally executed; external review is PENDING_REVIEW.

The model remains conditional on one frozen source, the finite declared candidate/context scope, first-service absorption and a conservative shared external boundary reservoir. Source share uncertainty is solved inside that model; these results do not establish a unique source attribution for all Ethereum history. No extra source is injected, and balances are not latest-state substitutes.

No full-sample collection, formal baseline/ablation campaign, general DEX, cross-chain extension or next stage was started. Stop at CHECKPOINT_1B_R3_REACHED. Any further work requires a new workorder; preserve this revision's files, old runs, budget identities and successful evidence.
