# 05 真实约束集成、映射与全零分配检查

外部验收状态：`PENDING_REVIEW`。本报告只公开实现说明和脱敏汇总；变量的真实身份、响应与逐行见证保留在 MIN。

## 实际执行链

本次已执行“保存响应 → 身份与哈希验证 → 整数事实归一化 → 块末锚点对齐 → 账户账本重放与对账 → 角色及约束映射 → LP → 精确与独立见证检查”。并非下载余额后继续调用 R2 全部初始余额为 `None` 的旧图。

`src/context_ledger_r3.py` 的 `replay_manifest` 验证 RPC 请求、响应、原始消息体、回执及区块身份，并验证 Dune 的冻结 SQL、执行身份、终态和完整分页，再调用 `assemble_model`。其真实输出 `model_input.json` 被 `src/context_lp_r3.py` 的 `run_context_document` 使用。

| 实际模型结构 | Atomic | Harmony |
|---|---:|---:|
| 已启用的初始实际余额上界 | 5 | 22 |
| 关闭窗口及中间显式锚点约束 | 5 | 23 |
| 重放产生的交易前后状态约束 | 34 | 198 |
| 物理来源变量（转移与费用） | 23 | 107 |
| 来源状态变量 | 80 | 414 |
| 全部 LP 变量 | 103 | 521 |
| 等式 | 80 | 414 |

34 与 198 个状态约束由既有真实锚点及完整收支重放产生，不是额外取得的 RPC 余额次数。55 个原始余额锚点全部有使用位置：27 个初始化、27 个关闭窗口状态、1 个中间状态。不存在“取得真实余额但统一丢回未知”的情况。

## 物理角色与共享约束

| 角色 | Atomic | Harmony | 进入模型的含义 |
|---|---:|---:|---|
| 冻结种子 | 1 | 1 | 固定单次来源注入 |
| 其他 R2 候选转移 | 11 | 49 | 单一容量，同时连接发送与接收状态 |
| 已建模账户间的额外相关流 | 0 | 1 | 两端共用同一个来源变量 |
| 已观察的外部流入，来源份额待求 | 0 | 6 | 保留实际金额，使用守恒的共享边界来源池 |
| 实际费用来源份额 | 11 | 50 | `[0, 实际费用]` 内求解并参与付款者守恒 |

Harmony 六条外部流入的实际金额已经取得。它们的来源份额未知是归因变量的正常状态，不是缺失实际账本数据。代码没有仅凭“不在候选图”就删除这些流入或写成无关资金；也没有为它们再注入一份种子。共享池只允许本模型已观察到的非服务边界出口所携来源返回，池内来源总量受同一个冻结种子和逐时守恒约束。该有限范围假设单独列于 `attribution_uncertainties.json` 及模型 assumptions。

Harmony-H21 的较早相关付款通过补洞接回同一账户状态，不再把付款端视为外部丢弃出口、收款端视为新资金。服务终点保持吸收边界，不采其后完整平台账本。对账户初始来源为 0 的因果初始化也明确限定在冻结候选范围内，不把它说成对 Ethereum 所有先前隐藏路径的证明。

Gas 按一笔物理交易一份费用去重。61 条模型付款者费用被应用；8 条外部付款者费用保留来源与未使用原因，接收者不代付。真实实际费用与涉源费用份额分开，后者由模型决定。root trace、DELEGATECALL 和失败回滚均不会制造额外正容量。

## 三个对照保持不同含义

`R2_BASELINE` 原样复算旧稀疏条件图。`BEST_AVAILABLE_CONTEXT` 使用本次所有已取得的可靠余额、账本、费用与状态约束。`MATCHED_INFORMATION_RELAXED` 在同一增强图中有意移除初始、显式锚点及重放状态的实际余额上界，保留变量、事件、来源、目标、等式、费用与顺序。

因此只有 BEST 与 MATCHED 的可行域嵌套用于单调性检查；全部已计算目标均通过。旧图与增强图的事件集合和费用端口不同，其金额差异另行解释，没有要求增强图端点机械服从旧图端点。服务联合区间直接以全部首次服务进入事件为一个共同目标求解，不相加独立地址的上界。

| 全部首次服务进入联合范围，ETH | BEST_AVAILABLE_CONTEXT | MATCHED_INFORMATION_RELAXED |
|---|---|---|
| Atomic | `[359.495, 359.495]` | `[0, 359.495]` |
| Harmony | `[504.907298683760128997, 1554.999042315467482]` | `[0, 1554.999042315467482]` |

这些是所声明模型内的条件区间；出现正下界或点区间不是取证停止条件，也不构成唯一司法来源结论。

## 真实约束为何排除旧的全零下游分配

求解器单独检查所有非种子候选来源变量同时为 0 是否可行。两个 BEST 模型均排除了它，两个 MATCHED 模型均保留了全零可行见证。该检查没有把跨多次转移的来源量相加当成独立资金本金。

Atomic 的五个真实初始余额均为 0，完整窗口中没有另一笔背景入款。来源不能被无限留在未知余额状态中。精确见证的守恒式为：

`359.505457948863919325 = 359.495 + 0.005482101864876 + 0.004975846999043325 ETH`

右侧分别是首次服务进入、实际来源费用份额和五账户期末来源余额。期末余额有真实上界；这些上界与费用共同排除了“所有下游都为 0，但种子一直留在未知账户余额里”的旧可行分配。

Harmony 包含已知其他资金、六条完整保留的外部入款以及一条相关账户间流，来源分配仍有实质不确定性。联合下界的实际精确证书把冻结种子、13 项实际状态容量、41 项费用上界和 2 项候选边容量组合为：

`2000 − 1250.073172890571651003 − 0.01952842566822 − 245 = 504.907298683760128997 ETH`

这是已保存对偶界贡献的汇总，不是全体账户余额相加，也不是把 41 项费用上界当作所有交易最终实际来源费用的账单。其对应下界分配同时通过独立整数/有理数账本检查。即使允许正常资金与来源资金共同参与所有已观察收支，时序、实际结余容量、转移容量和费用仍要求至少上述来源金额进入服务集合。数据没有被删减以制造该结论。

## 逐条映射与复核

MIN 的 `derived/context_pipeline/<query>/constraint_provenance.json` 从事实指向使用原因和约束；`derived/context_amounts/<query>/BEST_AVAILABLE_CONTEXT_CONSTRAINT_PROVENANCE.json` 进一步给出具体变量、等式、上下界、实际容量及证据身份。`CONTEXT_AMOUNT_RESULTS.json` 保留进入事件、地址—资产和全部服务联合区间、端点分配、精确检查、独立审计、同图比较以及全零检查。

`src/context_lp_r3.py` 的独立分配审计重新按事件顺序检查来源守恒、真实容量和费用，不仅复读求解器成功标志。证据到模型的合成接口测试也实际经过保存响应和哈希绑定：余额 20、种子 80、转出 90 得到 `[70,80]`；同图移除余额信息得到 `[0,80]`。该证据链与最终 ZIP 的离线复跑共同接受外部审查，当前不自行宣布外部验收通过。
