# Final R2 reference policy view

The final applied label versions are labels001, labels002, and labels003. Labels002 adds no service identity. Labels003 adds five HTX service identities, bringing the R2 changed identity cohort to seven addresses. The final output is `derived/reference_delta_r2/labels_003_final`; earlier output versions remain historical.

| Finite reference measure | R1 | Final R2 |
|---|---:|---:|
| B relations | 2284 | 1624 |
| C relations | 2272 | 1612 |
| Unresolved relations | 6473 | 7133 |
| Rejected association | 802 | 802 |
| Outside current target scope | 871 | 871 |

660 downstream registered certificates now encounter a first identified service and lose B/C eligibility under the unchanged policy. This is 579 more than the initial labels001 delta. There is no new chain data in this calculation, and no reference path is used for candidate acquisition. The final paper denominator remains unfrozen.

Only 15 registered Harmony reference queries were recomputed: 6,244 potentially affected relation rows using 3,369 registered event rows. The old-label control reproduces the R1 decisions with zero mismatches. The remaining 4,186 relation rows are inherited by canonical hashes and a minimal metric projection. The standalone included-slice verification passes under both old and final labels and reconstructs the global metrics without the full original library.

For the final actual fixed graphs, separate saved-endpoint arithmetic inspection confirms Atomic's six-entry address—ETH joint interval [0,359.495]. Harmony has seven service addresses and eight unique service-entry events; its 13 collector service states are arrival states, not 13 distinct transfers. Its address—ETH upper endpoints are 550, 249.997530999558463, 268, 225, 275, 251, and 5 ETH, all with zero lower endpoints. These remain conditional on unknown balances and declared observation gaps; the different addresses' upper bounds are not claimed to be one jointly optimized aggregate.
