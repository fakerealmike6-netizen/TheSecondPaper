# Stage1C方法和比较口径V1

本文是本轮新授权的实施定义，落实已定研究章程的“有界可达、Poison、Haircut；联合约束与协议续追两项消融”。它不是对某一外部软件原样复现的声明，也不改动主方法已经验收的来源语义。

## 1. 公共观察层

固定query的精确种子/金额/资产、事件身份和实际顺序、声明的任务/工程边界、冻结服务标签、所有相关实际流与锚点、实际Gas、可用协议语义。每个事件区分：源注入、合法候选传播、已建模账户内部context、明确正常外来流、未确定外部回流、边界出口、费用、目标吸收。

所有方法可读相同观察事实；主方法利用的可靠余额不能对基线故意隐藏。方法自行决定使用哪些事实/约束，记录input usage。不得暴露隐藏来源分配、Oracle端点、主方法结果或参考答案给baseline算法。

不能把所有上下文流递归扩展为新前沿，也不能因为一条流来自context就认定来源0。增强图内本来允许承接/传递来源的边界端口沿用主模型。服务目标始终为合法候选进入事件，不新认context对手方为目标。

## 2. FULL_INTERVAL

复用已验收主求解器和其真实context接口，不能为实验另写一套未经证据核对的主模型。

设完整可行集合为F，x_e为事件来源量。目标地址a同资产的联合来源量 y_a=Σ_{e进入a}x_e，区间[L_a,U_a]=[min_F y_a,max_F y_a]。全体服务联合目标是Σ_a y_a在**同一个F**中求界。不同资产不相加；求解非最优/数值不确定不伪装0。

FULL的地址输出为有精确可验证U_a>0的地址；L_a>0只标模型范围内稳健关联，不认定司法身份。

## 3. BOUNDED_REACHABILITY

使用与主任务相同的冻结合法时间/资产传播域、首次服务终止与已支持语义端口，作事件时序上的可达搜索。放弃来源金额容量、混入余额上界及全局耦合；正常外来流不构成新种子。源接收即目标的零跳合法。

这是在共同观察域上的上限式地址集合，不是无时间无资产的裸地址BFS。输出地址/进入事件和可达见证；金额字段为null，不能填实际交易额冒充来源量。

离线比较不重新调用各方法自己的provider。因本轮用固定图，不据此声称真实网络成本更低。保留当前source/outside-reservoir的时序端口，不创造新的种子输入。

## 4. POISON

事件状态沿时序传播布尔污染标记。种子来源标记为真；从已标记来源状态发出的合法正量流被标为污染，再标记接收状态。普通正常流不触发源标记；协议续接仅使用共同已支持语义。该简单基线不利用主LP可行端点或余额清零证明额外过滤。

对被标记的事件，名义污染量设为其实际容量v_e，未标记设0。这个名义量不保证全图来源守恒，不能当成一个可行来源分配向量，更不能标记为可信金额上界区间。以`POISON_NOMINAL_RAW`字段单独报告。

BOUND/POISON地址集合可能完全相同；不得为了展示差异引入随机阈值、Top-K或改定义。名义合计超过种子体现其粗粒度语义，不说程序写错或所有传统方法都违反守恒。

## 5. HAIRCUT

### 5.1 已知账本内基本分摊

每个账户—资产实际可用余额b、关联余额s独立维护，0≤s≤b。期初s=0且按可靠初始实际余额设置b；唯一源事件强制注入Q。对实际转出v，来源份额h=v*s/b（b>0），随后发送方扣b和s，接收方分别加v与h。v=0按0处理；b=0却v>0是账本/时点错误，不能除零或填未知点。

同一时点/交易先后依当前模型已有账本约定；Gas由实际付款方按该费项时点余额比例扣除，只扣一次，不从ERC20来源量扣ETH Gas。同交易多输出按同一份发送余额顺序处理或等价比例批处理，不能各自使用未更新余额导致超支。

明确正常流入关联量0须有主输入的正常来源依据；未确定外来流不得仅因非参考边设0。服务吸收、费用损耗、期末余额和协议边界出口都保留。

1:1 WETH操作使用与主模型相同的净输入输出归因规则；不能把额外协议中间调用当新注入。未支持复杂协议不强作Haircut支持。

用Fractions等精确有理数或明确误差控制；理论份额可以小于1 raw unit，本项目的连续LP允许分数来源量，不应无依据逐转移向下取整。

### 5.2 两个真实图的外部池补全约定

目前真实模型容许已流出来源经外部池返回，但没有观测外部池总正常余额。为给Haircut一个可复现的点，**本轮明示采用H_BMIN_BOUNDARY_V1约定**，不把它写入FULL模型或当链上锚点：

沿已固定观察顺序，令O_k为到时点k累计实际边界流出（从建模域进入outside），I_k为累计未确定外部流入（从outside返回建模域）。设
B0_out=max(0,max_k(I_k-O_k))，S0_out=0。

只为这个边界池补入B0_out单位“其他资金”，使已观察的流入流出时间序列可支付；池按普通Haircut的实际余额与来源余额比例进行分摊。费用和服务吸收不算可再回流的池存款；明确正常流入走其source-zero端口，不混作源注入。

B0_out是根据完整观察顺序得到的**最小可行账本补全约定**，不是已测余额、不是外部来源量真值，也不是唯一合理Haircut边界。闭合无此类边界的Atomic无需该补全。输出每个真实query是否采用该约定、B0_out、依据序列和来源守恒检查。

这让baseline使用同一观察事实，并公开其不可避免的点分配约定；不宣称它是某论文唯一标准定义。不要用主LP挑选有利B0，亦不调参数使Haircut更差。

若实际输入还有该补全未覆盖的硬约束，导致点不可行，保存问题和目标支持状态，不能clip到FULL区间或用求解器重新选个点叫Haircut。该点的feasibility checker可调用主约束检查器，只用于核验，不用于产生分配。适用域以已实现/有测试者为准。

## 6. NO_CROSS_TARGET_COUPLING

目标集合a_1...a_m按地址—资产冻结。构造每个a一份F副本F^(a)，各副本内部仍有同一笔种子的注入、事件容量、全部账本与Gas；跨副本不再要求同一物理事件的来源分配相等。

F_ind = Π_a F^(a)，J_ind = Σ_a y_a^(a)。单目标界与FULL单目标界应相同；联合界等于相应独立副本最值的和。FULL的单一向量在各副本重复形成可行嵌入，因此这是可证明的耦合放宽。

此消融研究“独立可行情形是否能作为一个共同情形使用”。不把它称为改错的LP、不指责原单目标优化，更不把独立上界和解释为保证可达的总额。报告：实际解绑的共享身份/等式、联合区间差、FULL中能否同时实现独立端点组合、一个受控共同容量冲突见证。

主代码可以按可分性分别求解以节约成本；至少一个小型样本实际构建复制变量模型，独立验证分解结果。只换原模型的objective而没有定义/实现F_ind，不算完成本消融。

## 7. NO_PROTOCOL_CONTINUATION与折叠等价

从同一raw观察图出发，拒绝跨协议输出续追，将净输入来源留在对应边界/损耗或已知退款账户，不从模型中凭空删除。用它与已认证语义折叠比较，而不是拿稀疏数据与完整数据对比。

在新受控WETH族及已认证单组件上运行；两个ETH主例没有此类已连接转换时如实标无作用/不适用。组件输入来源份额可作为合成参数alpha或变量，不把固定150 ETH全数认作真实涉案种子；保持“观察操作金额”和“合成来源分配”区分。

折叠前后在同一线性归因规则下检查边界多维可行关系：单输出、全部联合、不同权重目标；小整数样本枚举可行边界集合或使用独立代数对应。1:1 WETH可以给出映射方程。若只有有限目标相等，结论只写覆盖这些方向的验证，不夸大成任意投影完全等价证明。

## 8. BALANCE_INFORMATION_REMOVED

直接在同一增强模型移除选定余额锚点/上界等信息，保留种子、事件容量、明确时序、来源守恒、其他已知约束及同一目标。记录具体移除项；验证F_full⊆F_relaxed的逻辑与数值对照。不能删除一笔交易来假装只删除信息，也不能把放宽下界0标成主模型失败。

## 9. 输出与解释限制

每个方法至少有method_version、query/sample_id、input_fact_hash、scope_hash、label_version、applicability、status、output_kind、event/address列表和相应数值单位。单次运行失败/未执行不变成无目标0。

真实参考只作已知正例召回；没有来源真值则金额误差=null。控制真值有两个层次：一份隐藏可行分配和第二算法给出的可行端点，不互相替代。所有明显不支持场景在支持矩阵中列清，不能从方法分母消失。

本轮不承诺主方法地址更多/更少、下界必正、耗时更低或必优于任何baseline。价值是否表现为更窄区间、更可靠联合关系、正确语义续接，由实际结果说明。
