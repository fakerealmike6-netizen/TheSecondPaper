Run the actual R4 portable validator as shown in README.md. It creates a separate output mirror, strips credentials, forbids network, verifies manifest/equivalence/closure source hashes and executes all synthetic tests. Private mode additionally rebuilds saved evidence, verifies R2/R3 same-input comparisons, checks independent 48 intervals/100 witnesses, and replays both old and enhanced WETH evidence. Required failures propagate to nonzero exit.
