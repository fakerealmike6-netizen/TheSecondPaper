# WETH certification

Status: **NOT_CERTIFIED**. The inherited local deposit-pattern checks remain 6/12. This R1 made one real historical-code request; PublicNode returned HTTP 403. No runtime, trace, source or balance evidence was invented, and the blocked endpoint was not repeatedly retried.

The intended scope is one 150 ETH canonical WETH deposit sub-operation at block 20582941, not the surrounding bridge/LI.FI transaction. The repaired validator rejects explicit transaction, chain, block, log and source contradictions; hashes or arbitrary bytecode alone cannot certify mainnet. Synthetic normal controls are separate from real certification and cannot enable a real conversion edge. Same-asset conditional LP work continues independently.
