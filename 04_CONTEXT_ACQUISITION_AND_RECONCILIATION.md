# 04 金额上下文取得、对齐与对账

外部验收状态：`PENDING_REVIEW`。本报告使用匿名账户代号，原始逐行证据仅在 MIN 内提供。

## 完成范围

从固定 R2 的物理事件清单独立重算，Atomic 有 5 个、Harmony 有 22 个非终端 ETH 账户；两图 SHA-256、账户项数、必要事件位置与附件机械定位表全部匹配。匿名代号 Atomic-A01—A05、Harmony-H01—H22 对应 `derived/CONTEXT_TARGETS_ENHANCED.json` 各查询账户队列的行序。本次没有重新选择种子、扩大候选深度、重启标签采集或扫描服务终点之后的平台账本。

| 实际入模与核对项目 | Atomic | Harmony |
|---|---:|---:|
| 非终端账户 | 5 | 22 |
| 取得且入模的余额锚点 | 10 | 45 |
| 已知初始余额项 | 5 | 22 |
| 关闭窗口的余额锚点 | 5 | 22 |
| 保留并入模的额外中间锚点 | 0 | 1 |
| 模型物理 ETH 转移 | 12 | 57 |
| 账户—交易账本行 | 17 | 99 |
| 模型内付款者实际费用 | 11 | 50 |
| 明确排除的外部付款者费用 | 1 | 7 |
| 历史空代码观察 | 5 | 23 |
| 末锚整数 wei 零残差账户 | 5/5 | 22/22 |
| 未解决实际证据缺口 / 事实冲突 | 0 / 0 | 0 / 0 |

两个查询均达到 `FULL_CONTEXT_WITHIN_DECLARED_MODEL_SCOPE`。这要求数据类型、连续区间及全部分页覆盖成立，同时余额对账通过；没有以一次净残差为零代替收支完整性证明。候选前沿收口、原生账本完整、模型可求解和外部验收仍分别陈述。

## 新取得与原缓存复用

R2 已缓存的 12 个 Atomic、57 个 Harmony 唯一原生价值事件全部复用，R3 增加的唯一价值事件 ID 为 0。R3 的主要新增内容是历史状态、完整账户窗口的证据覆盖和事实入模，而非宣称又发现了同一批转移。

三份新完成的定向上下文结果共导出 140 行：Atomic 主窗口 24 行，Harmony 主窗口 114 行，Harmony 有限补洞窗口 2 行；每份结果均完成一页完整导出。行数包含交易行与 root trace 行，以及补洞结果中重复出现的同一物理交易。归一化后只有 69 笔物理交易及 69 个价值转移，root trace 不再次增加价值容量。

新查询覆盖必要账户完整边界块及其间连续区间，包括失败和零 value 顶层交易、选中交易的完整调用树、账户支付的费用，以及适用的提款、费用接收者与协议原生变化。实际结果没有新增失败或零 value 交易，也没有相关提款、费用接收者或协议原生变化命中；这些结论来自已完成的相应查询范围，不能由正金额候选转移清单推出。

历史 RPC 提供 55 个固定区块余额锚点、对应区块身份以及 28 个历史代码观察。28 次代码观察覆盖 27 个账户，另含 Harmony-H21 提前初始化的位置；全部为观察时点空代码。余额是固定历史块末结果，不是 latest，也没有把块末余额直接改名为某交易紧前余额。实际成功响应与此前缓存、调用树及 Gas 字段结合使用，失败的响应尝试没有生成余额事实。

## Harmony-H21 的 31 块补洞

原始最早候选事件位于 16402822，但主上下文结果显示 Harmony-H21 已在 16402791 向另一已建模账户付款。若仍保留原窗口，该笔已知关联流的一端会错误落到模型外，来源守恒关系也会被削弱。

因此仅补取 16402791—16402821 的 31 个区块，并取得 16402790 块末余额作为更早的初始化锚点。完整窗口现为 16402791—16402885；原先 16402821 块末余额保留为中间约束，其重放差额为 0。新增补洞结果只再次证实已观察的那笔交易及 root trace，没有另开递归候选前沿。

原始定位表保持不变。窗口调整、取证依据和独立区间覆盖分别见 `derived/CONTEXT_TARGETS.json`、`derived/CONTEXT_TARGETS_ENHANCED.json` 及 `configs/CONTEXT_REPLAY_R3.json`。

## 余额与费用对账方法

每个账户从自己的初始块末锚点开始，按已知区块号、交易位置和调用树顺序重放收支。种子只在冻结的种子事件处注入一次；锚点未包含本次稍后注入的种子。完整重放之后，再与关闭窗口的块末余额逐项比较，全部运算使用整数 wei。

69 条物理 Gas 事实均有映射：61 条由模型内账户实际支付，8 条由种子付款者或其他外部付款者支付并明确记为不扣模型接收者余额。Atomic 模型账户实际支付 `0.005482101864876 ETH`，Harmony 支付 `0.023476573773828 ETH`。实际费用与其中属于冻结来源的份额分开；来源份额由 LP 求解。

实际相关费用交易均为普通 ETH 转移。手续费在交易 value 之前从付款者状态扣除，转出金额与费用使用同一原有余额；没有用该交易稍后的入款融资较早费用。完整对账也没有补入虚构的“差额转账”或浮点容差。

## 审查定位与范围限定

MIN 中每个查询的 `derived/context_pipeline/<query>/` 包含 `balance_anchors.json`、`account_ledgers.json`、`ledger_reconciliation.json`、`account_code_evidence.json`、`receipt_cross_checks.json`、`evidence_gaps.json` 和 `source_manifest.json`。其中 `<query>` 为 `atomic_simple_transfer` 或 `harmony_high_branch`。实际重放入口是 `src/context_ledger_r3.py` 的 `replay` 命令，输入为 `configs/CONTEXT_REPLAY_R3.json`。

声明范围仍为固定单一种子、有限账户与窗口、首次服务进入后终止的模型。完整上下文不是全 Ethereum 路径证明，也不消除来源分配本身的不确定性。WETH 独立组件不参与本报告的同资产账本完整性判断。
